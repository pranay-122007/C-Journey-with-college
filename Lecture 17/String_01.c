//string inp or opt;
#include<stdio.h>
int main()
{
	char str[20];
	printf("enter the string: ");
	//scanf("%s",str);
	gets(str);//this is used only in string
	//printf("string are :%s\n",str);
	puts(str);
}
